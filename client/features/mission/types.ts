export type DailyMissionCopy = {
  back: string;
  today: string;
  prevDay: string;
  nextDay: string;
  play: string;
  playedToday: string;
  cards: {
    locations: string;
    compare: string;
    daily: string;
  };
  aria: {
    screen: string;
    back: string;
    dayPicker: string;
  };
};
